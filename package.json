{
  "name": "contracts",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "compile": "rm -rf build/* && yarn truffle compile --all",
    "test": "truffle test --network test",
    "deploy:dev": "truffle migrate --reset --network development",
    "deploy:sepolia": "truffle migrate --reset --network sepolia",
    "deploy:ethereum": "truffle migrate --reset --network ethereum"
  },
  "dependencies": {
    "@truffle/hdwallet-provider": "^2.1.13",
    "dotenv": "^16.3.1",
    "truffle": "^5.11.0",
    "truffle-assertions": "^0.9.2"
  }
}

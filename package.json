{
  "name": "contracts",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "compile": "rm -rf build/* && yarn truffle compile",
    "test": "truffle test"
  },
  "dependencies": {
    "truffle": "^5.11.0",
    "truffle-assertions": "^0.9.2"
  }
}
